{
  "name": "chip8-ts",
  "module": "src/index.ts",
  "type": "module",
  "scripts": {
    "console": "bun app/console.ts",
    "web": "bun app/web.ts",
    "test": "bun test",
    "rom-test": "bun test --todo --only"
  },
  "devDependencies": {
    "@types/bun": "latest"
  },
  "peerDependencies": {
    "typescript": "^5.0.0"
  }
}